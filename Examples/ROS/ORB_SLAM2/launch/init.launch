<?xml version="1.0"?>
<launch>
    <arg name="video_device" default="/dev/video1"/>
    <arg name="scan_topic" default="/cloud/scan"/>

    <!-- turn on webcam -->
    <!--
    <node name="usb_cam" pkg="usb_cam" type="usb_cam_node" output="screen">
       <param name="video_device" value="$(arg video_device)"/>
    </node>
    -->

    <!-- convert pointcloud to laser scan -->
    <node name="pt_to_ls" pkg="pointcloud_to_laserscan" type="pt_to_ls_node" output="screen">
       <remap from="scan" to="$(arg scan_topic)"/>
    </node>

    <!-- run gmapping -->
    <!--
    <include file="$(find mapping_ros)/launch/gmapping.launch">
       <arg name="scan_topic" default="$(arg scan_topic)"/>   
    </include>
    -->
</launch>
